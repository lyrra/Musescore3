find_path(VORBISFILE_INCLUDE_DIR vorbis/vorbisfile.h)

if (MINGW)
      set(CMAKE_FIND_LIBRARY_SUFFIXES ".dll.a")
else (MINGW)
      set(CMAKE_FIND_LIBRARY_SUFFIXES ".lib")
endif (MINGW)
find_library(VORBISFILE_LIBRARY NAMES libvorbisfile)

message(STATUS "VORBISFILE INC: ${VORBISFILE_INCLUDE_DIR}")
message(STATUS "VORBISFILE LIB: ${VORBISFILE_LIBRARY}")

if (VORBISFILE_INCLUDE_DIR AND VORBISFILE_LIBRARY)
      set(VORBISFILE_FOUND TRUE)
endif (VORBISFILE_INCLUDE_DIR AND VORBISFILE_LIBRARY)

if (VORBISFILE_FOUND)
      message (STATUS "Found vorbisfile: ${VORBISFILE_LIBRARY}")
else (VORBISFILE_FOUND)
      message (FATAL_ERROR "Could not find: vorbisfile")
endif (VORBISFILE_FOUND)
